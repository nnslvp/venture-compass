---
description: Read-only expansion of the last consilium — each lens's full return plus the synthesizer's reasoning. Does not re-run anything.
---

Expand the **last consilium** for the user, using the `managing-microventures` skill. This is
**read-only — do not re-run** the panel or re-dispatch any lens.

Show, in Russian:
1. **Each lens's full returned block** (VERDICT / CONFIDENCE / EVIDENCE / KILL-TRIGGER WATCH), named
   with its emoji — ⏳ 👾 💼 🕳 ⚔️ 🃏 (and the Gate 2 Адвокат смерти / Адвокат жизни split if it ran).
2. **The ⚖️ synthesizer's reasoning:** where the lenses agreed, where they conflicted, any lens it
   overruled **and the specific fact that justified overruling it**, and whether a Delphi round ran
   (and what changed).

**Scope:** the full lens returns are held in **this session's** context — they are not persisted to
`VENTURE.md`. If the last consilium ran in an earlier (now compacted) session, the full transcripts
are gone: reconstruct what you can from the `## Decisions` log (verdict + each lens's call + why) and
say plainly that the full per-lens transcripts for that gate are no longer available.

If no consilium has run yet this session, say so and point at `/audit`.
