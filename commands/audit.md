---
description: Force a Gate 2 checkpoint now — pull live metrics, run the blind consilium, return one honest verdict (manual override; checkpoints normally fire automatically).
argument-hint: "[area to weight, e.g. demand | tech | money | channels]"
---

Run a **Gate 2 · Чекпоинт** for the current venture **right now**, as a manual override. Use the
`managing-microventures` skill and `reference/consilium-protocol.md` + `reference/gate-playbook.md`.

Steps:
1. Reconstruct context from `VENTURE.md` + `LANDSCAPE.md` (silently).
2. **Pull live metrics** from `## Источник метрик` — never use a self-reported number. If the source
   is unconfigured, run `metrics-engineer` first.
3. Build the neutral brief and dispatch the Gate 2 lenses **blind and in parallel**: ⏳ keeper-of-time,
   👾 tech-skeptic, 💼 business-pragmatist, 🕳 demand-scout, the ⚔️ devils-advocate **Жизнь vs Смерть**
   pair, and 🃏 lateral.
4. If `$ARGUMENTS` names an area (demand / tech / money / channels), tell the relevant lens to weight
   it harder — but do **not** bias the verdict.
5. Synthesize via ⚖️ synthesizer (Mediating Assessments Protocol), run a Delphi round only on sharp
   conflict.
6. Write the verdict + each lens's call to `## Decisions`. KILL or a committed PIVOT is written as
   **proposed** and requires the user's explicit "да" before it's confirmed.
7. Surface only: **verdict + 2–4 reasons + the panel** (Russian). Hold transcripts for `/details`.
